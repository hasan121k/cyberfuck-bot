{
  "name": "firebase-html-bot",
  "version": "1.0.0",
  "description": "Running HTML Bot 24/7 with Puppeteer",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^22.0.0"
  }
}
